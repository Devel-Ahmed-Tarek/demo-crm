<?php

return [

    /*
    |--------------------------------------------------------------------------
    | دعم تعليم المحادثة كمقروءة (mark seen)
    |--------------------------------------------------------------------------
    */
    'mark_read_supported' => env('WHATSAPP_MARK_READ_SUPPORTED', false),

    /*
    |--------------------------------------------------------------------------
    | فترة الاستطلاع (poll) لواجهة /customer/messages — بالملّي ثانية
    |--------------------------------------------------------------------------
    */
    'poll_interval_ms' => (int) env('WHATSAPP_POLL_INTERVAL_MS', 1000),

];

<?php

return [

    /*
    |--------------------------------------------------------------------------
    | أمان: مفتاح Bearer / X-Api-Key — من .env
    |--------------------------------------------------------------------------
    */
    'token' => env('LEAD_API_TOKEN'),

    /*
    |--------------------------------------------------------------------------
    | إن تعذر مطابقة النص لأي صف في lead_sources / lead_stages
    |--------------------------------------------------------------------------
    */
    'default_source_key' => env('LEAD_API_DEFAULT_SOURCE_KEY', 'other'),
    'default_stage_key' => env('LEAD_API_DEFAULT_STAGE_KEY', 'new-client'),

];
